# 线性代数 人工审阅记录

## 记录说明
- 旧版“包含 Markdown 标题即需人工确认”的判断已废弃，不再作为审阅依据。
- 当前采用的新标准是：优先修正数学事实错误、明显不严谨表述、结构与内容类型严重错位的问题。
- 纯风格差异（如是否使用 H1 / H2、是否使用 Callout）在没有充分理由时不改。

## 已完成：行列式

### 已直接修正
- `线性代数/行列式/克拉默法则.md`
	- 原文把 `$D=0$ 且所有 `$D_k=0$` 直接写成“有无穷多解”，这个结论不成立；已改为“需结合秩判定”。
	- 将“微分关系”改为“关系”。
- `线性代数/行列式/全排列.md`
	- 修正“全排列”定义，明确它是对 `1,2,\dots,n` 的排列，而不是“方阵的 n 个元素的排列”。
- `线性代数/行列式/对换.md`
	- 修正明显错字。
	- 修正行列式按排列展开的公式，补全为 `$a_{1p_1}a_{2p_2}\cdots a_{np_n}$`。
- `线性代数/行列式/范德蒙德行列式.md`
	- 替换掉原文中已明确标注“这里计算不对，重算”的错误推广段。
	- 改写为正确的 `$n\times n$` 推广形式：从 `$k$` 次开始时可按行提取 `$x_i^k$`。
- `线性代数/行列式/行列式.md`
	- 修正“某一行/列数乘，值不变”的错误说法，改为“行列式也数乘该系数”。
	- 修正末尾关于逆序数的说明语句与损坏公式。
	- 去除残留的 `==...==` 标注。

### 本轮未改动
- `线性代数/行列式/---行列式---.md`
	- 作为目录页可正常工作，当前未发现必须修改的问题。
- `线性代数/行列式/拉普拉斯展开法.md`
	- 内容基本成立，当前未见需要立即修正的事实性错误。
- `线性代数/行列式/行列式性质.md`
	- 内容简短但正确，当前不作风格性扩写。
- `线性代数/行列式/逆序数.md`
	- 作为极短词条可接受，当前不强制扩写。

### 暂记观察项
- `线性代数/行列式/范德蒙德行列式.md`
	- “证明”部分目前更像说明性草稿，不够严谨，但暂未发现我能在不大幅重写的前提下保证更好的版本，因此先保留。
- `线性代数/行列式/行列式.md`
	- 后半段“行列式、全排列、对换和逆序数”仍偏讲义式，结构较杂，但当前内容已可读，先不做结构性重写。

## 下一步
- 继续补扫尚未细看的个别文件，但线性代数主要章节的第一轮实质性检查已完成。

## 已完成：向量组

### 已直接修正
- `线性代数/向量组/向量组及其线性组合.md`
	- 修正线性组合公式中的笔误 `$k_ma_m`，去掉重复且错误的旧公式。
- `线性代数/向量组/向量组的秩.md`
	- 原文件几乎为空，已补成最小正确定义，并关联到矩阵秩与高斯消元。
- `线性代数/向量组/一次独立.md`
	- 修正错误外链（原来指向与线性代数无关的页面）。
	- 将“任意向量组都可用格拉姆矩阵判定”的表述收紧到内积空间语境。
- `线性代数/向量组/正交向量组.md`
	- 补上“正交向量组线性无关”所需前提：向量必须都是非零向量。

### 暂记观察项
- `线性代数/向量组/极小相关子集.md`
	- 当前内容可用，但偏应用化，后续若统一风格可以进一步收敛到“定义 + 判据 + 一个例子”的结构。

## 已完成：相似变换

### 已直接修正
- `线性代数/相似变换/特征值.md`
	- 修正例子结论，明确“矩阵不可对角化”是针对整个矩阵，而不是只对单个特征值说“完全可对角化”。
- `线性代数/相似变换/特征多项式.md`
	- 修正定义，改为多项式 `$p_A(\lambda)=\det(A-\lambda I)$`，而不是把“等于 0”写进定义里。
- `线性代数/相似变换/正定矩阵.md`
	- 修正 `$B^TAB$` 的结论：只有当 $B$ 列满秩时才能保证正定，否则至多半正定。
- `线性代数/相似变换/代数重数.md`
	- 补成最小正确定义。
- `线性代数/相似变换/几何重数.md`
	- 补成最小正确定义，并补上几何重数不超过代数重数。
- `线性代数/相似变换/相似变换矩阵.md`
	- 修正定义，说明一般相似变换矩阵是任意可逆矩阵；“由特征向量按列组成”只是在可对角化时的常见情形。
- `线性代数/相似变换/二次型.md`
	- 修正示例对角化后的标准型结果，原结果计算错误。
- `线性代数/相似变换/正定二次型.md`
	- 删除错误的“面积扩大 8 倍”推导段，避免把等值面的右端常数与面积变化机械等同。
- `线性代数/相似变换/相似变换的一般步骤.md`
	- 修正“不可对角化示例”中整套错误计算：原矩阵特征值根本不是二重根。
	- 改成标准 Jordan 块示例，并重写广义特征向量步骤。

### 暂记观察项
- `线性代数/相似变换/正交对角化.md`
	- 内容整体成立，但篇幅较长、教学风格较重；当前未发现必须立即修正的硬错误。
- `线性代数/相似变换/复特征根.md`
	- 内容来源较完整，当前未发现明确错误，暂不改写。

## 已完成：线性空间

### 已直接修正
- `线性代数/线性空间/线性变换.md`
	- 补上“矩阵表示线性变换”需要选定基、通常在有限维下讨论的前提。
- `线性代数/线性空间/维数.md`
	- 收紧为有限维语境下的标准定义。
- `线性代数/线性空间/坐标.md`
	- 从“点的几何坐标”改成“向量相对于一组基的坐标”，更符合本章语境。
- `线性代数/线性空间/基变换.md`
	- 修正基变换公式方向，避免把坐标变换方向写反。
- `线性代数/线性空间/坐标变换.md`
	- 从一般几何坐标变换改成线性代数语境下的“同一向量在不同基下的坐标转换”，避免和非线性坐标系变换混在一起。
- `线性代数/线性空间/零空间.md`
	- 修正定义句与 `RREF` 笔误。
- `线性代数/线性空间/正交性.md`
	- 将“点积为零”改为更一般的“内积为零”，并修正积分写法。
- `线性代数/线性空间/正交投影.md`
	- 补上公式成立所需的前提：所取基必须是正交基；若是标准正交基可进一步简化。
- `线性代数/线性空间/正交补空间.md`
	- 补上维数公式与直和分解所需的有限维前提。
- `线性代数/线性空间/范数.md`
	- 修正三角不等式公式。
	- 补全 `dlink`，并修正非负性表述。
- `线性代数/线性空间/线性独立.md`
	- 修正损坏的行列式计算结果与目录归属。
- `线性代数/线性空间/希尔伯特空间.md`
	- 修正 `dlink`。
	- 把“元素唯一表示为基底元素或其倍数的和”改成“按标准正交基展开为收敛级数”，避免无穷维语境下的误导。

### 暂记观察项
- `线性代数/线性空间/直和.md`
	- 当前更像一般代数结构介绍，而不是线性空间条目；但内容未见明显错误，先保留。
- `线性代数/线性空间/对偶空间.md`
	- 内容正确但偏百科式，后续若统一风格可以压缩。

## 已完成：补充

### 已直接修正
- `线性代数/补充/Matroid的基.md`
	- 修正“有两个基”后又写“这是唯一的最大独立集”的自相矛盾表述。

## 已完成：矩阵

### 已直接修正
- `线性代数/矩阵/矩阵.md`
	- 补上“矩阵表示线性变换”所需的前提：需要选定基，且通常讨论有限维情形。
	- 去掉无依据的主观性表述，改成更中性的结构说明。
- `线性代数/矩阵/转置.md`
	- 修正“与求逆运算的关系”表述，改为标准的“若 $A$ 可逆，则 $A^T$ 可逆，且 $(A^{-1})^T=(A^T)^{-1}$”。
- `线性代数/矩阵/LUD.md`
	- 修正 LU 分解说明，明确并非任意矩阵都能直接写成 `$A=LU$`，很多情况下应写成 `$PA=LU$`。
- `线性代数/矩阵/QRD.md`
	- 修复了原文截断的定义句。
	- 修正目录归属到 `---矩阵---`。
	- 补齐 QR 分解适用语境与逆矩阵公式的前提。
- `线性代数/矩阵/SVD.md`
	- 将正文改为“紧致 SVD”口径，使维度说明与原例子保持一致。
- `线性代数/矩阵/代数余子式.md`
	- 修正代数余子式公式，避免把 `$M_{ij}$` 和 `det(M_{ij})` 混用。
- `线性代数/矩阵/余子式.md`
	- 修正余子式定义，改为标准的“任取 $k$ 行与 $k$ 列得到 $k$ 阶子矩阵，其行列式称为 $k$ 阶余子式”。
- `线性代数/矩阵/伴随矩阵.md`
	- 去除残留 `==...==` 标注。
	- 重新表述伴随矩阵的构造步骤：先成余因子矩阵，再转置。

### 暂记观察项
- `线性代数/矩阵/矩阵乘法.md`
	- 当前写法偏“按列-按行分解”的讲义式说明，可读但不够紧凑；暂未发现必须立即修正的事实性错误。
- `线性代数/矩阵/矩阵分块法.md`
	- 内容正确，但示例固定为 `8\times 8` 与 `4\times 4` 子块，偏举例而非通式；先保留。

## 已完成：线性方程组

### 已直接修正
- `线性代数/线性方程组/主元.md`
	- 修正目录归属到 `---线性方程组---`。
- `线性代数/线性方程组/正规方程.md`
	- 修正错误推理：原文把“方阵可逆时两种方法结果相同”错误地解释成 `$A^TA=A$` 与 `$A^Tb=b$`；已改为正确推导。
- `线性代数/线性方程组/矩阵的秩.md`
	- 修正“满秩”定义，改为 `$rank(A)=\min(m,n)$`，并区分行满秩、列满秩。
- `线性代数/线性方程组/线性方程组的解.md`
	- 修正例题计算结果：原文 `$x=1,y=1$` 错误，已改为 `$x=4/7,y=9/7$`。
	- 顺手修正重复编号与“高斯-赛德尔”译名。

### 暂记观察项
- `线性代数/线性方程组/---线性方程组---.md`
	- 目录页中 `Intro / What / Why / How` 的英文标题属于风格选择，目前不影响内容正确性，先不改。
- `线性代数/线性方程组/高斯消元法.md`
	- 内容整体可用，但示例推演更像讲义草稿，后续若统一整理教学风格，可以再重写。