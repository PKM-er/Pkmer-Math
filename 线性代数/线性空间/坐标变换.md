---
tags:
  - 数学
dlink:
  - "[[--线性代数--]]"
  - "[[---线性空间---]]"
aliases:
  - Coordinate Transformation
author:
  - Cyletix
  - GPT-4
---
#### Introduction
在线性代数中，坐标变换是指同一个向量在不同基下的坐标表示之间的转换。

#### Definition
设同一个向量 $v$ 在旧基下的坐标为 $[v]_B$，在新基下的坐标为 $[v]_{B'}$。若新基向量在旧基下的坐标按列组成矩阵 $P$，则有：
$$
[v]_B = P[v]_{B'},\qquad [v]_{B'} = P^{-1}[v]_B.
$$

#### Properties
- **依赖于基**：坐标变化来自基的变化，向量本身并没有改变。
- **可逆性**：基与基之间的坐标变换矩阵一定可逆。

#### Examples
- 若在 $\mathbb{R}^2$ 中取旧基为标准基 $B$，新基为
$$
B' = \left\{\begin{bmatrix}1\\1\end{bmatrix},\begin{bmatrix}1\\-1\end{bmatrix}\right\},
$$
则
$$
P=\begin{bmatrix}1&1\\1&-1\end{bmatrix}
$$
给出两组坐标之间的关系。

#### Applications
坐标变换在物理学、工程学和图形学中广泛应用；在线性代数中，它是理解[[基变换]]、[[相似变换]]和矩阵表示的基础。
