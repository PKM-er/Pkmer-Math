---
tags:
  - 数学
dlink:
  - "[[---相似变换---]]"
---
> [!NOTE] 半正定矩阵
> 实对称矩阵 $A$ 称为半正定矩阵，若对所有 $x\in\mathbb{R}^n$ 都有 $x^TAx\ge 0$，等价地其特征值全部非负（$\lambda_i\ge 0$）。与[[正定矩阵]]的区别在于允许零特征值：存在非零 $x$ 使 $x^TAx=0$ 时，$A$ 半正定但非正定，且因包含零特征值而不可逆。例如 $A=\begin{bmatrix}4&2\\2&1\end{bmatrix}$ 的特征值为 $5$ 与 $0$，是半正定但非正定矩阵。
