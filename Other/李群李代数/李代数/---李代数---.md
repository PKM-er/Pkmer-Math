---
tags:
  - 数学
  - 目录
dlink:
  - "[[--李群李代数--]]"
author:
  - DeepSeekV4Flash
chapter: 3
---

> [!quote] 核心思想
> [[李代数]]是李群在单位元处的切空间，[[李括号]]（对易子）度量交换次序的差异、编码群的非交换性；[[李群与李代数的对照]]列出常见李群与李代数的一一对应，体现"全局对称与局部无穷小"是同一结构的两个侧面。

# 目录
1. [[李代数]]
2. [[李括号]]
3. [[李群与李代数的对照]]

# 自动索引

```base
filters:
  and:
    - file.name != this.file.name
    - or:
        - file.folder == this.file.folder
        - 'file.hasProperty("dlink") && dlink.contains(this.file.name)'
views:
  - type: list
	name: 列表
```

# 孤立笔记

```base
filters:
  and:
    - file.folder == this.file.folder
    - file.name != this.file.name
    - or:
        - '!file.hasProperty("dlink")'
        - '!dlink.contains(this)'
views:
  - type: list
	name: 列表
```
